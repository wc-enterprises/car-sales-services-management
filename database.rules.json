{
    "rules": {
        ".read": "auth != null && (auth.uid === 'FIRST_USER_UID' || auth.uid === 'SECOND_USER_UID')",
        ".write": "auth != null && (auth.uid === 'FIRST_USER_UID' || auth.uid === 'SECOND_USER_UID')"
    }
}
